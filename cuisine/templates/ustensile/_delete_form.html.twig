<form method="post" action="{{ path('app_ustensile_delete', {'id': ustensile.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ustensile.id) }}">
    <button class="btn">Delete</button>
</form>
